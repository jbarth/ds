package com.ubs.gtp.data.resource.mappers;

import org.codehaus.jackson.map.JsonSerializer;
import org.codehaus.jackson.map.ObjectMapper;

/**
 * Custom {@link ObjectMapper} that registers custom {@link JsonSerializer}.
 *
 * @author Jakub D Kozlowski
 * @since 0.3
 */
public final class CustomObjectMapper extends ObjectMapper {

    /**
     * Empty constructor.
     */
    public CustomObjectMapper() {
//        SimpleModule module = new SimpleModule("data-rest", new Version(0, 3, 0, "SNAPSHOT"));
//        module.addSerializer(new CustomDateSerializer());
//        this.registerModule(module);
    }
}